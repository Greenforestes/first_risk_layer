

```
local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 15.0
      publish_frequency: 10.0
      global_frame: odom
      robot_base_frame: base_footprint
      use_sim_time: True
      rolling_window: true
      width: 3.0
      height: 3.0
      resolution: 0.05
      robot_radius: 0.12
      transform_tolerance: 0.1
      always_send_full_costmap: True

      plugins: ["obstacle_layer", "inflation_layer"]

      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        footprint_clearing_enabled: True
        combination_method: 1
        observation_sources: scan
        scan:
          topic: /scan
          data_type: "LaserScan"
          clearing: True
          marking: True
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          max_obstacle_height: 2.0
          min_obstacle_height: 0.0
          observation_persistence: 0.0
          expected_update_rate: 0.0

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: True
        cost_scaling_factor: 3.0
        inflation_radius: 0.15
```



```
local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 15.0
      publish_frequency: 10.0
      global_frame: odom
      robot_base_frame: base_footprint
      use_sim_time: True
      rolling_window: true
      width: 3.0
      height: 3.0
      resolution: 0.05
      robot_radius: 0.12
      transform_tolerance: 0.1
      always_send_full_costmap: True

      plugins: ["obstacle_layer", "risk_layer", "inflation_layer"]

      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        footprint_clearing_enabled: True
        combination_method: 1
        observation_sources: scan
        scan:
          topic: /scan
          data_type: "LaserScan"
          clearing: True
          marking: True
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          max_obstacle_height: 2.0
          min_obstacle_height: 0.0
          observation_persistence: 0.0
          expected_update_rate: 0.0

      risk_layer:
        plugin: "nav2_risk_layer::RiskLayer"
        enabled: True
        risk_cost: 255
        risk_radius: 0.15
        min_risk_cost: 80

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: True
        cost_scaling_factor: 3.0
        inflation_radius: 0.15
```

2026.4.12
看dynamic_actor_manager.py和nav2_risk_layer的全部
为什么：risk_layer:

​        plugin: "nav2_costmap_2d::RiskLayer"

​        enabled: True
就能直接找到我在另一个功能包编写的插件了

key:也就是说，当前最优策略是：

1. 让 risk layer 能收到 `future_path`
2. 先把 future path 的点简单写到 local costmap 上
3. 在 RViz 里先看到“future 点对应的风险区”
4. **确认整条链通了以后，再加时间衰减和空间扩散**

不做复杂高斯风险场

不做时间衰减公式

